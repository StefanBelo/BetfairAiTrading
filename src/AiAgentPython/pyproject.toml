[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-agent-python"
version = "0.1.0"
description = "AI Agent for Bfexplorer"
dependencies = [
    "fast-agent",
]
requires-python = ">=3.8"